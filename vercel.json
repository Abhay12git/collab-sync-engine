{
  "buildCommand": "npm install --ignore-scripts && npm run build --workspace=shared && npm run build --workspace=frontend",
  "outputDirectory": "frontend/dist",
  "installCommand": "npm install --ignore-scripts",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
